## Python packaging toolchain

Inside the `smartai` virtual environment, pip, setuptools, and wheel
are upgraded to current versions to ensure reliable installation of
audio, speech-to-text, LLM, and embedding libraries on Android (ARM64).
